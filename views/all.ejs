<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Links</title>
</head>
<body>

    <style>

        body{
            font-family: sans-serif;
        }

        h1{
            font-size: 35pt;
        }

        #addLink{
            text-decoration: none;
            color: black;
            margin-bottom: 20px;
            border: 2px solid black;
            border-radius: 10px;
            width: 100px;
            height: 20px;
            padding: 10px;
            text-align: center;
        }

        div{
            margin-top: 20px;
            width: 400px;
            height: 150px;
            border: 2px solid black;
            border-radius: 10px;
            padding: 5px;
        }

        #title{
            font-size: 25pt;
            text-decoration: none;
            color: black;
        }

        p{
            font-size: 15pt;
        }

        .btn{
            width: 80px;
            height: 20px;
            background-color: black;
            color: white;
            border-radius: 10px;
            border: none;
            cursor: pointer;
        }

        .btn:active{
            transform: scale(0.95);
        }


        #edit{
            text-decoration: none;
            color: white;
        }

        @media screen and (max-width: 600px){

            #title{
            font-size: 15pt;
            text-decoration: none;
            color: black;
            }

            p{
            font-size: 8pt;
        }
        
        .btn{
            width: 60px;
            height: 20px;
            background-color: black;
            color: white;
            border-radius: 10px;
            border: none;
            cursor: pointer;
        }

            div{
            margin-top: 20px;
            width: 300px;
            height: 100px;
            border: 2px solid black;
            border-radius: 10px;
            padding: 5px;
        }
        }
    </style>

    <h1>Links</h1>
    <a id="addLink" href="/">Add Link</a>

    <%for(let i=0; i < links.length; i++){%>
        <div id="<%=links[i].id%>">
            <form method="POST" action="/?_method=DELETE">
            <a id="title" href="<%=links[i].url%>" target="_blank"><%=links[i].title%></a>
            <p><%=links[i].description%></p>
            <input name="id" value="<%=links[i].id%>" hidden>
            <button class="btn" type="submit">Delete</button>
            <button class="btn"><a id="edit" href="/edit/<%=links[i].id%>">Edit</a></button>
        </form>
        </div>
    <%}%>

  
</body>
</html>